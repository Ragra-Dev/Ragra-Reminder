__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/

# Ragra local data - never commit tokens, credentials, or the personal DB
.env
.env.local
.env.production
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite3
CLAUDE.md
data/
logs/
*.log
*token*.json
*credential*.json
*client_secret*.json
*.key
*.pem
secrets/

.pytest_cache/

# Conversation/session exports - never source, may contain private context
*-session.txt
*.session.txt
raw-chats/
# ragra-session.txt is the exception: a curated, public project handoff
# doc (not a raw export) - explicitly tracked despite matching the
# wildcard above.
!ragra-session.txt

# Private developer diary - personal notes, never for the public repo
ragra-dev-diary.txt
